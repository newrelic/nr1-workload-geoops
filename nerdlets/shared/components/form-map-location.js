import React from 'react';
import PropTypes from 'prop-types';

export default class FormMapLocation extends React.PureComponent {
  static propTypes = {
    //
  };

  constructor(props) {
    super(props);
    this.state = {
      //
    };
  }

  componentDidMount() {
    //
  }

  render() {
    return (
      //
      <></>
    );
  }
}
